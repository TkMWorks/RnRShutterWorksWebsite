# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json
*.lock.hcl
*.tfplan
*.zip
/src/tests/*.jpg

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc
/src/ImageUploadUtility/UploadUtility/.vs/ProjectEvaluation
/src/ImageUploadUtility/UploadUtility/.vs/UploadUtility/CopilotIndices/18.0.912.14514
/src/ImageUploadUtility/UploadUtility/.vs/UploadUtility.slnx/DesignTimeBuild/.dtbcache.v2
/src/ImageUploadUtility/UploadUtility/.vs/UploadUtility.slnx/FileContentIndex
/src/ImageUploadUtility/UploadUtility/.vs/UploadUtility.slnx/v18
/src/ImageUploadUtility/UploadUtility/bin/Debug/net10.0-windows
/src/ImageUploadUtility/UploadUtility/obj
/src/ImageUploadUtility/UploadUtility/.editorconfig
